# Assignment A1 — Environment Setup & Your First AI Calls
**CSCI 250 · Fall 2026 · Week 1 · 6 points · Due Sunday Aug 30, 2026, 11:59 PM PST**

## Goal
Stand up your AI development environment (Colab + API keys + Ollama), refresh core Python, and make your very first calls to **Claude**, **Gemini**, and a **local Ollama** model.

## What you'll use
- `code/01_python_review.ipynb` — Python refresher (types, control flow, functions, comprehensions).
- `code/02_first_ai_calls.ipynb` — first calls to Claude (`anthropic`), Gemini (`google-generativeai`), and Ollama.
- Free accounts/keys: **console.anthropic.com** (Claude), **aistudio.google.com** (Gemini), **ollama.com** (local, no key). Store keys in the Colab **Secrets** panel — never paste a key into a shared cell.

## Tasks
1. **Set up.** Open both notebooks in Colab. Add your `ANTHROPIC_API_KEY` and `GEMINI_API_KEY` to the Colab Secrets panel and confirm the key-loading cell in Notebook 2 runs without errors.
2. **Python review.** In `01_python_review.ipynb`, complete the **"Exercises (complete these for A1)"** section: write the small functions/comprehensions requested and make every provided `assert` pass.
3. **First AI calls.** In `02_first_ai_calls.ipynb`, run the **shared prompt** through **Claude** (Section 3) and **Gemini** (Section 4). Paste both responses into the notebook.
4. **Local model.** Install Ollama, pull a small model (e.g. `llama3.2`), and run the **Ollama** cell (Section 5) on the same prompt. If you cannot run Ollama locally, document what you tried and the error — effort counts here.
5. **Compare (Section 6).** In 3–4 sentences, note one way the three responses differed (length, tone, accuracy, speed).
6. **Stretch:** Change the shared prompt to a task you care about, re-run Claude and Gemini, and add one sentence on which you'd pick and why.

## Deliverables
Submit on Canvas:
- Both completed notebooks (`.ipynb` files **or** shareable Colab links with output cells visible).
- Your short written **comparison** (Task 5) and the **stretch** note (Task 6) inside the notebook.
- A one-line **AI-Use note** (see below).

## Rubric (6 points)
| Criterion | Pts | Excellent | Adequate | Needs work |
|---|---:|---|---|---|
| **Correctness / it works** | 2.5 | Notebooks run cleanly; asserts pass; all three models called | Runs with minor issues | Doesn't run / wrong result |
| **Understanding** (your-words explanation) | 1.5 | Clear, choices justified | Mostly correct | Missing or copied |
| **Completeness** | 1.0 | All parts (+ stretch) | Core parts done | Major parts missing |
| **Craft & communication** (readable code, AI-Use note) | 1.0 | Clear & documented | Mostly clear | Hard to follow / no AI-Use note |

## Submitting
Submit on **Canvas** by the deadline. This is correctness-graded work — you may **revise once** (no penalty) after feedback. If an API or Ollama blocks you, submit what you have with notes; documented effort earns credit.

## AI-Use note (required)
One line: which AI tools you used and how (e.g., *"Used Claude to debug my Secrets-loading cell."*) — or **"none."**

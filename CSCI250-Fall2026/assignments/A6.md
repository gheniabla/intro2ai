# Assignment A6 — Train & Evaluate a Small Neural Network
**CSCI 250 · Fall 2026 · Week 6 · 6 points · Due Sunday Oct 4, 2026, 11:59 PM PST**

## Goal
Build, train, and evaluate a **tiny neural network**, watch the **loss go down** as it learns, and connect the pieces (neurons, layers, activations, loss, gradient descent) to the foundation under modern LLMs.

## What you'll use
- `code/01_tiny_neural_net.ipynb` — a small net on the **two-moons** dataset, trained with **PyTorch** (with a **NumPy fallback** that does the same thing by hand).
- `code/02_cnn_and_sequence_tour.ipynb` — a short, guided tour of convolution (vision) and sequences.

## Tasks
1. **Train the net.** In `01_tiny_neural_net.ipynb`, run the **two-moons** dataset and train the network (Section 2a PyTorch, or the **2b NumPy fallback** if you prefer/can't install PyTorch). Report the **final test accuracy**.
2. **Watch it learn.** Run Section 3 and include the **loss curve**. In 2–3 sentences explain what the falling curve means and what the **learning rate** controls.
3. **Experiment (Section 4).** Change **one** thing — number of hidden units, learning rate, or epochs — re-train, and report how test accuracy and the loss curve changed. State whether your change helped or hurt.
4. **Activations matter.** In your own words (2–3 sentences), explain why a network needs a **nonlinear** activation (e.g., ReLU) — what happens if you stack linear layers with no activation?
5. **Tour & connect.** Skim `02_cnn_and_sequence_tour.ipynb` and write one sentence on how the **softmax** that picks a class here relates to how an LLM picks the **next token**.
6. **Stretch:** Run a second experiment (change a different knob) and briefly compare the two runs — which setting generalized best?

## Deliverables
Submit on Canvas:
- The completed `01_tiny_neural_net.ipynb` (and your notes from `02_...`) as `.ipynb` files **or** Colab links with outputs and the loss curve visible.
- Your **final accuracy**, **loss-curve explanation**, **experiment result**, **activation explanation**, and **stretch** comparison inside the notebook.
- A one-line **AI-Use note**.

## Rubric (6 points)
| Criterion | Pts | Excellent | Adequate | Needs work |
|---|---:|---|---|---|
| **Correctness / it works** | 2.5 | Net trains; accuracy + loss curve reported | Runs with minor issues | Doesn't run / wrong result |
| **Understanding** (your-words explanation) | 1.5 | Clear, choices justified | Mostly correct | Missing or copied |
| **Completeness** | 1.0 | All parts (+ stretch) | Core parts done | Major parts missing |
| **Craft & communication** (readable code, AI-Use note) | 1.0 | Clear & documented | Mostly clear | Hard to follow / no AI-Use note |

## Submitting
Submit on **Canvas** by the deadline. This is correctness-graded work — you may **revise once** (no penalty) after feedback. If PyTorch won't install, use the NumPy fallback and document any blockers — effort counts.

## AI-Use note (required)
One line: which AI tools you used and how (e.g., *"Asked Claude why my loss wasn't dropping."*) — or **"none."**

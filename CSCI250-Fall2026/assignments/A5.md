# Assignment A5 — Regression & Classification with Proper Metrics
**CSCI 250 · Fall 2026 · Week 5 · 6 points · Due Sunday Sep 27, 2026, 11:59 PM PST**

## Goal
Train, evaluate, and compare supervised models on real data — **regression** with MAE/RMSE/R² and **classification** with accuracy, precision, recall, F1, and a confusion matrix — and recognize **overfitting**.

## What you'll use
- `code/01_regression.ipynb` — linear regression and `Ridge` on the **diabetes** dataset; predicted-vs-actual plot.
- `code/02_classification.ipynb` — logistic regression, k-NN, and decision trees; confusion matrix; decision-tree depth study; cross-validation.

## Tasks
**Part 1 — Regression** (`01_regression.ipynb`, "A5 — Part 1 tasks"):
1. Fit **linear regression** on **diabetes**; report **MAE, RMSE, and R²** on the test set.
2. Add **`Ridge`** and compare its metrics to plain linear regression in one sentence.
3. Make the **predicted-vs-actual** plot and say in one sentence what a perfect model would look like on it.

**Part 2 — Classification** (`02_classification.ipynb`, "A5 — Part 2 tasks"):
4. Train **all three** classifiers (logistic regression, k-NN, decision tree) and print a **`classification_report`** (accuracy, precision, recall, F1) for each.
5. Show the **confusion matrix** for your best classifier and name which classes get confused.
6. Explain **why accuracy can lie** on imbalanced data, in your own words (2–3 sentences).
7. Run the **decision-tree depth** study (Section 5): report train vs test accuracy at a shallow and a deep depth and identify the **overfitting** signal (the train↔test gap).

**Stretch:** Use `cross_val_score` (Section 6) to report a mean ± std accuracy for one model, and say one sentence on why it's more trustworthy than a single split.

## Deliverables
Submit on Canvas:
- Both completed notebooks (`.ipynb` files **or** Colab links with outputs and plots).
- Your **metric comparisons**, **"accuracy lies" explanation**, **overfitting finding**, and **stretch** note inside the notebook.
- A one-line **AI-Use note**.

## Rubric (6 points)
| Criterion | Pts | Excellent | Adequate | Needs work |
|---|---:|---|---|---|
| **Correctness / it works** | 2.5 | Runs cleanly; correct metrics reported | Runs with minor issues | Doesn't run / wrong result |
| **Understanding** (your-words explanation) | 1.5 | Clear, choices justified | Mostly correct | Missing or copied |
| **Completeness** | 1.0 | All parts (+ stretch) | Core parts done | Major parts missing |
| **Craft & communication** (readable code, AI-Use note) | 1.0 | Clear & documented | Mostly clear | Hard to follow / no AI-Use note |

## Submitting
Submit on **Canvas** by the deadline. (Thursday Sep 25 is Native American Day — the module is open and A5 is still due Sunday.) This is correctness-graded work — you may **revise once** (no penalty) after feedback.

## AI-Use note (required)
One line: which AI tools you used and how (e.g., *"Used Gemini to interpret a confusion matrix."*) — or **"none."**

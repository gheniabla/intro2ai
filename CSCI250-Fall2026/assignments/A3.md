# Assignment A3 — Data Visualization with Pandas & Matplotlib
**CSCI 250 · Fall 2026 · Week 3 · 6 points · Due Sunday Sep 13, 2026, 11:59 PM PST**

## Goal
Use **Pandas** to load, filter, and group tabular data, then build the four core **Matplotlib** charts (line, bar, scatter, histogram) — including a look at the **A–Z Handwritten** image dataset.

## What you'll use
- `code/01_pandas_basics.ipynb` — DataFrames, `.loc`/`.iloc`, boolean filtering, new columns, `groupby`, missing data.
- `code/02_visualization_AZ.ipynb` — the **A–Z Handwritten Digits/Letters** dataset: viewing characters, bar/histogram/scatter charts.

## Tasks
1. **Pandas basics.** In `01_pandas_basics.ipynb`, complete the **"Exercises (complete these for A3)"** section. Your work must include: a **boolean filter**, a **new computed column**, one **`groupby` aggregation**, and one **missing-data** handling step (`isna`/`dropna`/`fillna`) with a one-line note on *why* you chose drop vs fill.
2. **See the data.** In `02_visualization_AZ.ipynb`, run Sections 1–4 to load the dataset and display **one character** and a **3×3 grid**.
3. **Make charts (Section 8 "Your turn").** Produce at least **three** of the four chart types, each with a **title and axis labels**:
   - a **bar chart** of examples per letter,
   - a **histogram** of average brightness per image,
   - a **scatter** of brightness vs letter index (or a line chart of a trend you find).
4. **Read your charts.** In 3–4 sentences, describe one pattern you can see in the data from your charts (e.g., which letters are most common, how brightness is distributed).
5. **Stretch:** Add a **fourth** chart type, or use `groupby` to compute mean brightness per letter and plot it as a bar chart.

## Deliverables
Submit on Canvas:
- Both completed notebooks (`.ipynb` files **or** Colab links with charts rendered).
- Your **missing-data note** (Task 1), **chart interpretation** (Task 4), and **stretch** chart.
- A one-line **AI-Use note**.

## Rubric (6 points)
| Criterion | Pts | Excellent | Adequate | Needs work |
|---|---:|---|---|---|
| **Correctness / it works** | 2.5 | Runs cleanly; charts correct and labeled | Runs with minor issues | Doesn't run / wrong result |
| **Understanding** (your-words explanation) | 1.5 | Clear, choices justified | Mostly correct | Missing or copied |
| **Completeness** | 1.0 | All parts (+ stretch) | Core parts done | Major parts missing |
| **Craft & communication** (readable code, AI-Use note) | 1.0 | Clear & documented; titled/labeled charts | Mostly clear | Hard to follow / no AI-Use note |

## Submitting
Submit on **Canvas** by the deadline. (Monday Sep 7 is Labor Day — the module is open and A3 is still due Sunday.) This is correctness-graded work — you may **revise once** (no penalty) after feedback.

## AI-Use note (required)
One line: which AI tools you used and how (e.g., *"Used Claude to fix a Matplotlib label."*) — or **"none."**

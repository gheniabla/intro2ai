# Assignment A2 — Data Processing with NumPy
**CSCI 250 · Fall 2026 · Week 2 · 6 points · Due Sunday Sep 6, 2026, 11:59 PM PST**

## Goal
Use **NumPy** to create, reshape, and inspect arrays, replace loops with **vectorized** operations, and run **axis-based aggregations** to analyze a small dataset.

## What you'll use
- `code/01_numpy_basics.ipynb` — arrays, `shape`/`dtype`/`ndim`, reshaping, vectorization, broadcasting, masks, axis ops.
- `code/02_numpy_analytics.ipynb` — a self-contained student dataset loaded with `np.genfromtxt`; means, masks, `argmax`, standardization, correlation.

## Tasks
1. **Basics.** In `01_numpy_basics.ipynb`, complete the **"Exercises (complete these for A2)"** section. Make every provided `assert` pass — including a reshape, a boolean mask, and one `axis=` aggregation.
2. **Vectorize.** Find the `%timeit` cell in Section 4 and report (in a comment or markdown cell) how much faster the vectorized version was than the Python loop, and explain in one sentence *why*.
3. **Analytics.** In `02_numpy_analytics.ipynb`, run Sections 1–7, then complete **Section 8 "Your analytics (for A2)":**
   - Compute the **mean of each column** with `axis=0`.
   - Use a **boolean mask** to select rows that meet a condition you choose (e.g., exam score above the mean) and report how many rows matched.
   - Use **`argmax`** to identify the top row, and **standardize** one feature column with broadcasting `(x - mean) / std`.
4. **Explain.** In 2–3 sentences (your own words), explain what **broadcasting** is and give one example from your code where it saved you a loop.
5. **Stretch:** Compute the **correlation** between two columns (Section 7 pattern) and write one sentence interpreting whether they move together.

## Deliverables
Submit on Canvas:
- Both completed notebooks (`.ipynb` files **or** Colab links with outputs visible).
- Your **timing observation** (Task 2), **broadcasting explanation** (Task 4), and **stretch** note inside the notebook.
- A one-line **AI-Use note**.

## Rubric (6 points)
| Criterion | Pts | Excellent | Adequate | Needs work |
|---|---:|---|---|---|
| **Correctness / it works** | 2.5 | Runs cleanly; asserts pass; right results | Runs with minor issues | Doesn't run / wrong result |
| **Understanding** (your-words explanation) | 1.5 | Clear, choices justified | Mostly correct | Missing or copied |
| **Completeness** | 1.0 | All parts (+ stretch) | Core parts done | Major parts missing |
| **Craft & communication** (readable code, AI-Use note) | 1.0 | Clear & documented | Mostly clear | Hard to follow / no AI-Use note |

## Submitting
Submit on **Canvas** by the deadline. This is correctness-graded work — you may **revise once** (no penalty) after feedback.

## AI-Use note (required)
One line: which AI tools you used and how (e.g., *"Asked Gemini to explain an axis error."*) — or **"none."**

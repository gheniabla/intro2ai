# Assignment A4 — The scikit-learn ML Workflow
**CSCI 250 · Fall 2026 · Week 4 · 6 points · Due Sunday Sep 20, 2026, 11:59 PM PST**

## Goal
Run a complete supervised-ML workflow in **scikit-learn** — **load → split → fit → evaluate → predict** — and understand the cardinal rule: never judge a model on data it trained on.

## What you'll use
- `code/01_ml_workflow.ipynb` — the end-to-end loop on a bundled dataset (get data → explore → split → fit → evaluate → predict).
- `code/02_sklearn_basics.ipynb` — the universal `fit`/`predict` estimator API and `train_test_split`.
- Bundled sklearn datasets: `load_iris` (classification) and `load_diabetes` (regression).

## Tasks
1. **Workflow.** In `01_ml_workflow.ipynb`, run all sections end-to-end on the provided dataset, then **explain the cardinal rule** in your own words (Section 6): why is the score on the **training** set misleading? Reproduce the demonstration that train accuracy looks better than test accuracy.
2. **The estimator API.** In `02_sklearn_basics.ipynb`, complete the **"Try it yourself"** section: load `iris` with `train_test_split` (`test_size=0.2`, `random_state=42`, `stratify=y`), fit a `KNeighborsClassifier`, and report **test accuracy** via `model.score(...)`.
3. **Swap the model.** Re-run the same four-line pipeline with a **different** estimator (e.g., `DecisionTreeClassifier`). Report both test accuracies and note in one sentence that the API barely changed.
4. **Why split?** In 2–3 sentences explain what `train_test_split` does and what `random_state` and `stratify` are for.
5. **Stretch:** Switch tasks to **regression** — load `diabetes`, fit `LinearRegression`, and report the built-in `.score()` (R²). One sentence: how does evaluating regression differ from classification?

## Deliverables
Submit on Canvas:
- Both completed notebooks (`.ipynb` files **or** Colab links with outputs).
- Your **cardinal-rule explanation** (Task 1), **two accuracies** (Tasks 2–3), **split explanation** (Task 4), and **stretch** result.
- A one-line **AI-Use note**.

## Rubric (6 points)
| Criterion | Pts | Excellent | Adequate | Needs work |
|---|---:|---|---|---|
| **Correctness / it works** | 2.5 | Pipeline runs; correct test scores reported | Runs with minor issues | Doesn't run / wrong result |
| **Understanding** (your-words explanation) | 1.5 | Clear, choices justified | Mostly correct | Missing or copied |
| **Completeness** | 1.0 | All parts (+ stretch) | Core parts done | Major parts missing |
| **Craft & communication** (readable code, AI-Use note) | 1.0 | Clear & documented | Mostly clear | Hard to follow / no AI-Use note |

## Submitting
Submit on **Canvas** by the deadline. This is correctness-graded work — you may **revise once** (no penalty) after feedback.

## AI-Use note (required)
One line: which AI tools you used and how (e.g., *"Asked Claude to explain stratify=y."*) — or **"none."**
